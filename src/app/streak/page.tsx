import StreakGame from '@/components/StreakGame';

export const metadata = {
  title: 'Streak Mode – Athlete Origins',
  description: 'How many athletes can you place in a row? Guess the country before the timer runs out.',
};

export default function StreakPage() {
  return <StreakGame />;
}
